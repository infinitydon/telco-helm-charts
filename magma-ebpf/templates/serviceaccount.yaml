{{ include "magma-ebpf.assertNoLatest" . }}
{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "magma-ebpf.serviceAccountName" . }}
  labels:
    {{- include "magma-ebpf.labels" . | nindent 4 }}
{{- end }}
