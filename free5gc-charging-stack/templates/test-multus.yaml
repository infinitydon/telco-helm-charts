{{- if .Values.stackSmokeTest.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-stack-smoke
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  activeDeadlineSeconds: 120
  serviceAccountName: {{ .Release.Name }}-stack-test
  restartPolicy: Never
  nodeSelector:
    {{- toYaml .Values.labNodeSelector | nindent 4 }}
  containers:
    - name: curl
      image: curlimages/curl:8.20.0
      env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
      command:
        - sh
        - -ceu
        - |
          token="$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)"
          ca=/var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          curl -fsS http://{{ .Release.Name }}-free5gc-charging-portal:{{ (index .Values "free5gc-charging-portal").service.port }}/healthz
          curl -fsS http://{{ .Release.Name }}-user-charging-portal:{{ .Values.userPortal.service.port }}/healthz
          curl -fsS -H "{{ .Values.userPortal.config.trustedSubscriberHeader }}: imsi-208930000000001" http://{{ .Release.Name }}-user-charging-portal:{{ .Values.userPortal.service.port }}/api/me >/tmp/me.json
          grep -q imsi-208930000000001 /tmp/me.json
          api="https://${KUBERNETES_SERVICE_HOST}:${KUBERNETES_SERVICE_PORT_HTTPS}/apis/k8s.cni.cncf.io/v1/namespaces/${POD_NAMESPACE}/network-attachment-definitions"
          curl -fsS --cacert "$ca" -H "Authorization: Bearer $token" "$api" >/tmp/nads.json
          grep -q n2network /tmp/nads.json
          grep -q n3network /tmp/nads.json
          grep -q n4network /tmp/nads.json
          grep -q n6network /tmp/nads.json
{{- end }}
