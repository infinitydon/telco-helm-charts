Magma full-stack upstream chart installed.

NMS GUI:
  kubectl -n {{ .Release.Namespace }} get svc magmalte
  http://<node-ip>:<magmalte-nodeport>/user/login

Orc8r API/controller services:
  kubectl -n {{ .Release.Namespace }} get svc {{ .Release.Name }}-nginx-proxy {{ .Release.Name }}-clientcert-nginx bootstrapper-orc8r-nginx

Default NMS login:
  organization: {{ .Values.nmsAdmin.organization }}
  username: {{ .Values.nmsAdmin.email }}
  password: {{ .Values.nmsAdmin.password }}

AGW should be installed separately with magma-agw-upstream and pointed at:
  controller.{{ .Values.global.domainName }}
  bootstrapper-controller.{{ .Values.global.domainName }}
